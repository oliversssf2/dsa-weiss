// struct MyVector<T, const N: usize> {
    // data: Box<[T; N]>,
    // the_size: usize,
    // the_capacity: usize,
// }

// impl<T, const N: usize> MyVector<T, N> {
    // fn new(size: usize) -> MyVector<T, N>{
        // MyVector { data: (), the_size: (), the_capacity: () }
    // }
// }




fn main() {
}